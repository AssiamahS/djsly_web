- 2026-09-15 stems: demucs default = CPU, pegs all 8 cores (30s clip: 51s wall/72s cpu). `-d mps` + `--segment 7` (int, htdemucs max 7.8) + `-j 1` + nice = 18s wall/7s cpu. The mac stem server had no X-Hash in Access-Control-Allow-Headers → the Pages site could only use the cloud queue. Load avg 103 seen today came from another session's playwright headless chrome (mic.py, software GPU process at 500%+), not demucs.
